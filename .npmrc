# Format and publish settings for the pnpm workspace.
#
# Peer dependencies are a core part of the DSH plugin model, so they are
# auto-linked rather than hard-failed. Individual packages may override the
# default registry through their own publishConfig.
engine-strict=false
